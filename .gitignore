node_modules/
frontend/node_modules/
frontend/dist/
standalone-ssp-builder/node_modules/
standalone-ssp-builder/dist/
.wrangler/
.dev.vars
*.zip
*.docx
.DS_Store
Thumbs.db

# Environment files — may contain secrets (JWT_SECRET, API keys, etc.)
# Only intentionally checked-in env files are un-ignored below.
.env
.env.*
!.env.example
!.env.demo
!.env.production.example
# NOTE: .env.production was previously committed and should be purged from
# git history using BFG Repo-Cleaner or git filter-branch.
docker/.env

# Cached OSCAL data from NIST (downloaded by import scripts)
.oscal-cache/
