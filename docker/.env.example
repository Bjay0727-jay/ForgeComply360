# ============================================================================
# ForgeComply 360 - On-Prem Environment Configuration
# Forge Cyber Defense - SDVOSB
#
# Copy this file to .env and update values before deployment:
#   cp .env.example .env
# ============================================================================

# ── Security (REQUIRED) ─────────────────────────────────────────
# Generate with: openssl rand -hex 64
JWT_SECRET=

# Emergency access key for HIPAA break-glass procedures (REQUIRED)
# Generate with: openssl rand -hex 32
EMERGENCY_ACCESS_KEY=

# ── TLS Certificates ────────────────────────────────────────────
# Path to directory containing fullchain.pem and privkey.pem
# Generate self-signed for testing:
#   mkdir -p ./certs && openssl req -x509 -nodes -days 365 -newkey rsa:2048 \
#     -keyout ./certs/privkey.pem -out ./certs/fullchain.pem \
#     -subj "/CN=forgecomply.local"
TLS_CERT_DIR=./certs

# ── Ports ─────────────────────────────────────────────────────────
FRONTEND_PORT=443
API_PORT=8787
OLLAMA_PORT=11434

# ── CORS ──────────────────────────────────────────────────────────
# Set to your frontend URL (use https for production)
CORS_ORIGIN=https://localhost

# ── AI Model ──────────────────────────────────────────────────────
# Ollama model to use for ForgeML Writer
# Options: llama3.1:8b (default, 4.7GB), llama3.1:70b (for better quality, 40GB)
# For air-gapped: pre-pull the model before disconnecting
OLLAMA_MODEL=llama3.1:8b

# ── Backup (HIPAA 164.308(a)(7)) ────────────────────────────────
# Directory for automated database backups
BACKUP_DIR=./backups
# Retention period in days (HIPAA minimum: 6 years = 2190 days)
BACKUP_RETENTION_DAYS=2190
